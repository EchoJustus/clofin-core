# The smallest Makefile `check-disclaimer.sh` can check: a `help` target that
# prints the canonical sentence from the one file that holds it, exactly as the
# repository's own does. A fixture that restated the sentence would be testing
# the fixture.
.PHONY: help
help:
	@echo "clofin (fixture)"
	@echo ""
	@cat resources/disclaimer.txt
