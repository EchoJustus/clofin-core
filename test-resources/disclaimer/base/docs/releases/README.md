# Release annotations (fixture)

A minimal stand-in for `docs/releases/README.md`, carrying the one structure
`scripts/check-disclaimer.sh` parses: the historical-exemptions table.

### Historical exemptions

| Tag | Reason |
|---|---|
| `ref-0` | A fixture tag, published before the rule. |
